import { creativeStudioTransformHtml } from "./creativeStudioTransformHtml.js";

function injectAfter(html: string, marker: string, content: string): string {
  if (!html.includes(marker)) throw new Error(`CREATIVE_STUDIO_MULTISELECT_MARKER_MISSING: ${marker}`);
  return html.replace(marker, `${marker}${content}`);
}

function injectBefore(html: string, marker: string, content: string): string {
  if (!html.includes(marker)) throw new Error(`CREATIVE_STUDIO_MULTISELECT_MARKER_MISSING: ${marker}`);
  return html.replace(marker, `${content}${marker}`);
}

export function creativeStudioMultiSelectHtml(): string {
  let html = creativeStudioTransformHtml();

  html = injectAfter(
    html,
    '<div id="properties" class="hidden"></div>',
    `<div id="arrangePanel" class="section" style="margin-top:16px">
      <h3>Arrange</h3>
      <div id="multiSelectionSummary" class="note" style="margin-bottom:8px">Select two or more unlocked layers with Shift/Cmd/Ctrl-click.</div>
      <div class="toolbar-row"><button class="secondary" data-align="left" disabled>Left</button><button class="secondary" data-align="horizontal-center" disabled>Center</button><button class="secondary" data-align="right" disabled>Right</button></div>
      <div class="toolbar-row"><button class="secondary" data-align="top" disabled>Top</button><button class="secondary" data-align="vertical-center" disabled>Middle</button><button class="secondary" data-align="bottom" disabled>Bottom</button></div>
      <div class="toolbar-row"><button class="secondary" data-distribute="horizontal" disabled>Distribute H</button><button class="secondary" data-distribute="vertical" disabled>Distribute V</button></div>
      <div class="toolbar-row"><button id="groupSelectionBtn" class="secondary" disabled>Group</button><button id="ungroupSelectionBtn" class="secondary" disabled>Ungroup</button></div>
      <div class="toolbar-row"><button id="duplicateSelectionBtn" class="secondary" disabled>Duplicate</button><button id="deleteSelectionBtn" class="secondary danger" disabled>Delete</button></div>
      <div class="note">Multi-move, align, distribute, grouping, duplicate and delete are deterministic DesignDocument operations. One arrange action creates one persisted version and zero model calls.</div>
    </div>`,
  );

  const style = `<style>
    .layer-row.multi-selected{outline:1px solid #52c7ff;background:#17232a}
    #arrangePanel button:disabled{opacity:.38;cursor:not-allowed}
  </style>`;
  html = injectBefore(html, "</head>", style);

  const script = `<script>
(function(){
  'use strict';
  var NS='http://www.w3.org/2000/svg';
  var selectedIds=[],drag=null,raf=0;
  function $(id){return document.getElementById(id);}
  function project(){return window.__creativeStudioCurrentProject&&window.__creativeStudioCurrentProject();}
  function primary(){return window.__creativeStudioSelectedLayer&&window.__creativeStudioSelectedLayer();}
  function status(message,type){if(window.__creativeStudioSetStatus)window.__creativeStudioSetStatus(message,type);}
  async function api(path,options){var response=await fetch(path,Object.assign({headers:{'content-type':'application/json'}},options||{}));var data=await response.json();if(!response.ok)throw new Error(data.error||('HTTP '+response.status));return data;}
  window.__creativeStudioMultiSelectionIds=function(){return selectedIds.slice();};
  window.__creativeStudioSetMultiSelection=function(ids){selectedIds=(ids||[]).filter(function(id,index,list){return typeof id==='string'&&list.indexOf(id)===index&&Boolean(layerById(id));});if(selectedIds.length)selectPrimary(selectedIds[selectedIds.length-1]);schedule();return selectedIds.slice();};
  function doc(){var p=project();return p&&p.document;}
  function layerById(id){var d=doc();return d&&d.layers.find(function(layer){return layer.id===id;});}
  function chosen(){var d=doc();if(!d)return [];return selectedIds.map(function(id){return d.layers.find(function(layer){return layer.id===id;});}).filter(Boolean);}
  function bounds(layers){var left=Math.min.apply(null,layers.map(function(layer){return layer.x;}));var top=Math.min.apply(null,layers.map(function(layer){return layer.y;}));var right=Math.max.apply(null,layers.map(function(layer){return layer.x+layer.width;}));var bottom=Math.max.apply(null,layers.map(function(layer){return layer.y+layer.height;}));return {x:left,y:top,width:right-left,height:bottom-top};}
  function svgPoint(event){var svg=$('artboard');var point=svg.createSVGPoint();point.x=event.clientX;point.y=event.clientY;var matrix=svg.getScreenCTM();if(!matrix)return {x:0,y:0};var value=point.matrixTransform(matrix.inverse());return {x:value.x,y:value.y};}
  function schedule(){if(raf)return;raf=requestAnimationFrame(function(){raf=0;draw();});}
  function selectPrimary(id){var rows=Array.from(document.querySelectorAll('[data-select-layer]'));var row=rows.find(function(candidate){return candidate.dataset.selectLayer===id;});if(row)row.click();}
  function syncSingle(){if(selectedIds.length>1)return;var layer=primary();if(layer)selectedIds=[layer.id];}
  function parentGroup(id){var d=doc();return d&&d.layers.find(function(layer){return layer.type==='group'&&Array.isArray(layer.childLayerIds)&&layer.childLayerIds.includes(id);});}
  function arrangeable(layers){return layers.length>=2&&layers.every(function(layer){return !layer.locked&&layer.type!=='group'&&layer.type!=='background';});}
  function groupable(layers){return arrangeable(layers)&&layers.every(function(layer){return layer.type!=='logo'&&layer.type!=='mask'&&!parentGroup(layer.id);});}
  function multiMutable(layers,action){return layers.length>=2&&layers.every(function(layer){if(layer.locked||layer.type==='logo'||layer.type==='background'||layer.type==='group'||layer.type==='mask')return false;if(action==='delete'&&parentGroup(layer.id))return false;return true;});}
  function updateToolbar(){var layers=chosen(),canArrange=arrangeable(layers);document.querySelectorAll('[data-align]').forEach(function(button){button.disabled=!canArrange;});document.querySelectorAll('[data-distribute]').forEach(function(button){button.disabled=!(canArrange&&layers.length>=3);});var group=$('groupSelectionBtn'),ungroup=$('ungroupSelectionBtn'),duplicate=$('duplicateSelectionBtn'),remove=$('deleteSelectionBtn');if(group)group.disabled=!groupable(layers);if(ungroup)ungroup.disabled=!(layers.length===1&&layers[0].type==='group'&&!layers[0].locked);if(duplicate)duplicate.disabled=!multiMutable(layers,'duplicate');if(remove)remove.disabled=!multiMutable(layers,'delete');var summary=$('multiSelectionSummary');if(!summary)return;if(layers.length>1)summary.textContent=layers.length+' layers selected · drag to move together; smart guides snap to nearby layers and equal gaps.';else if(layers.length===1&&layers[0].type==='group')summary.textContent='Group selected · arrow keys move all members; resize/rotate handles transform the group.';else summary.textContent='Select two or more unlocked layers with Shift/Cmd/Ctrl-click or drag a marquee.';}
  function el(name,attrs){var node=document.createElementNS(NS,name);Object.keys(attrs||{}).forEach(function(key){node.setAttribute(key,String(attrs[key]));});return node;}
  function drawSmartGuides(svg,d,guides){var old=svg.querySelector('#studioSmartGuides');if(old)old.remove();if(!guides||!guides.length)return;var width=Math.max(1.5,d.artboard.width/720);var g=el('g',{id:'studioSmartGuides','pointer-events':'none'});guides.forEach(function(guide){if(guide.kind==='alignment'){if(guide.axis==='x')g.appendChild(el('line',{x1:guide.position,y1:guide.start,x2:guide.position,y2:guide.end,stroke:'#ff4fd8','stroke-width':width,'stroke-dasharray':'7 4'}));else g.appendChild(el('line',{x1:guide.start,y1:guide.position,x2:guide.end,y2:guide.position,stroke:'#ff4fd8','stroke-width':width,'stroke-dasharray':'7 4'}));return;}guide.segments.forEach(function(segment){if(guide.axis==='horizontal'){g.appendChild(el('line',{x1:segment.start,y1:segment.cross,x2:segment.end,y2:segment.cross,stroke:'#65d58a','stroke-width':width}));g.appendChild(el('line',{x1:segment.start,y1:segment.cross-6,x2:segment.start,y2:segment.cross+6,stroke:'#65d58a','stroke-width':width}));g.appendChild(el('line',{x1:segment.end,y1:segment.cross-6,x2:segment.end,y2:segment.cross+6,stroke:'#65d58a','stroke-width':width}));}else{g.appendChild(el('line',{x1:segment.cross,y1:segment.start,x2:segment.cross,y2:segment.end,stroke:'#65d58a','stroke-width':width}));g.appendChild(el('line',{x1:segment.cross-6,y1:segment.start,x2:segment.cross+6,y2:segment.start,stroke:'#65d58a','stroke-width':width}));g.appendChild(el('line',{x1:segment.cross-6,y1:segment.end,x2:segment.cross+6,y2:segment.end,stroke:'#65d58a','stroke-width':width}));}});});svg.appendChild(g);}
  function draw(){var d=doc(),svg=$('artboard');document.querySelectorAll('.layer-row.multi-selected').forEach(function(row){row.classList.remove('multi-selected');});selectedIds=selectedIds.filter(function(id){return Boolean(layerById(id));});selectedIds.forEach(function(id){var rows=Array.from(document.querySelectorAll('[data-select-layer]'));var row=rows.find(function(candidate){return candidate.dataset.selectLayer===id;});if(row)row.classList.add('multi-selected');});if(!svg){updateToolbar();return;}var old=svg.querySelector('#studioMultiSelectionControls');if(old)old.remove();drawSmartGuides(svg,d,drag&&drag.guides);var layers=chosen().filter(function(layer){return layer.visible;});if(layers.length>1){var size=Math.max(2,d.artboard.width/540);var g=el('g',{id:'studioMultiSelectionControls','pointer-events':'none'});layers.forEach(function(layer){var cx=layer.x+layer.width/2,cy=layer.y+layer.height/2;g.appendChild(el('rect',{x:layer.x,y:layer.y,width:layer.width,height:layer.height,fill:'none',stroke:'#52c7ff','stroke-width':size,'stroke-dasharray':'7 5',transform:'rotate('+layer.rotation+' '+cx+' '+cy+')'}));});var frame=bounds(layers);g.appendChild(el('rect',{x:frame.x,y:frame.y,width:frame.width,height:frame.height,fill:'none',stroke:'#52c7ff','stroke-width':Math.max(2,size*1.5),'stroke-dasharray':'12 7'}));if(drag)g.setAttribute('transform','translate('+drag.dx+' '+drag.dy+')');svg.appendChild(g);}updateToolbar();}
  function smartSnap(frame,x,y,d){var tolerance=Math.max(5,d.artboard.width*.006);var guides=[];var others=d.layers.filter(function(layer){return layer.visible&&!selectedIds.includes(layer.id)&&layer.type!=='background'&&layer.type!=='group'&&layer.type!=='mask';});var xCandidates=[],yCandidates=[];function addX(target,guide){var adjustment=target-x;if(Math.abs(adjustment)<=tolerance)xCandidates.push({adjustment:adjustment,guide:guide});}function addY(target,guide){var adjustment=target-y;if(Math.abs(adjustment)<=tolerance)yCandidates.push({adjustment:adjustment,guide:guide});}var safeX=d.artboard.width*.05,safeY=d.artboard.height*.05;addX(safeX,null);addX(d.artboard.width*.95-frame.width,null);addX(d.artboard.width/2-frame.width/2,null);addY(safeY,null);addY(d.artboard.height*.95-frame.height,null);addY(d.artboard.height/2-frame.height/2,null);others.forEach(function(other){var moveX=[x,x+frame.width/2,x+frame.width],targetX=[other.x,other.x+other.width/2,other.x+other.width];moveX.forEach(function(mv){targetX.forEach(function(tv){var adjustment=tv-mv;if(Math.abs(adjustment)<=tolerance)xCandidates.push({adjustment:adjustment,guide:{kind:'alignment',axis:'x',position:tv,start:Math.min(y,other.y),end:Math.max(y+frame.height,other.y+other.height),targetId:other.id}});});});var moveY=[y,y+frame.height/2,y+frame.height],targetY=[other.y,other.y+other.height/2,other.y+other.height];moveY.forEach(function(mv){targetY.forEach(function(tv){var adjustment=tv-mv;if(Math.abs(adjustment)<=tolerance)yCandidates.push({adjustment:adjustment,guide:{kind:'alignment',axis:'y',position:tv,start:Math.min(x,other.x),end:Math.max(x+frame.width,other.x+other.width),targetId:other.id}});});});});var left=others.filter(function(o){return o.x+o.width<=x+tolerance;}).sort(function(a,b){return (b.x+b.width)-(a.x+a.width);})[0],right=others.filter(function(o){return o.x>=x+frame.width-tolerance;}).sort(function(a,b){return a.x-b.x;})[0];if(left&&right){var available=right.x-(left.x+left.width)-frame.width;if(available>=0){var gap=available/2,target=left.x+left.width+gap,adjust=target-x;if(Math.abs(adjust)<=tolerance)xCandidates.push({adjustment:adjust,guide:{kind:'spacing',axis:'horizontal',gap:gap,segments:[{start:left.x+left.width,end:target,cross:y+frame.height/2},{start:target+frame.width,end:right.x,cross:y+frame.height/2}],neighborIds:[left.id,right.id]}});}}var top=others.filter(function(o){return o.y+o.height<=y+tolerance;}).sort(function(a,b){return (b.y+b.height)-(a.y+a.height);})[0],bottom=others.filter(function(o){return o.y>=y+frame.height-tolerance;}).sort(function(a,b){return a.y-b.y;})[0];if(top&&bottom){var availableY=bottom.y-(top.y+top.height)-frame.height;if(availableY>=0){var gapY=availableY/2,targetY=top.y+top.height+gapY,adjustY=targetY-y;if(Math.abs(adjustY)<=tolerance)yCandidates.push({adjustment:adjustY,guide:{kind:'spacing',axis:'vertical',gap:gapY,segments:[{start:top.y+top.height,end:targetY,cross:x+frame.width/2},{start:targetY+frame.height,end:bottom.y,cross:x+frame.width/2}],neighborIds:[top.id,bottom.id]}});}}xCandidates.sort(function(a,b){return Math.abs(a.adjustment)-Math.abs(b.adjustment);});yCandidates.sort(function(a,b){return Math.abs(a.adjustment)-Math.abs(b.adjustment);});if(xCandidates[0]){x+=xCandidates[0].adjustment;if(xCandidates[0].guide)guides.push(xCandidates[0].guide);}if(yCandidates[0]){y+=yCandidates[0].adjustment;if(yCandidates[0].guide)guides.push(yCandidates[0].guide);}return {x:Math.max(0,Math.min(d.artboard.width-frame.width,x)),y:Math.max(0,Math.min(d.artboard.height-frame.height,y)),guides:guides};}
  async function commit(operation,message,nextIds){var p=project();if(!p)return;var preserve=selectedIds.slice();try{var next=await api('/api/studio/operation',{method:'POST',body:JSON.stringify({designId:p.document.id,operation:operation})});window.__creativeStudioLoadProject(next);var desired=typeof nextIds==='function'?nextIds(next):(nextIds||preserve);selectedIds=desired.filter(function(id){return next.document.layers.some(function(layer){return layer.id===id;});});if(!selectedIds.length){var first=primary();if(first)selectedIds=[first.id];}if(selectedIds.length)selectPrimary(selectedIds[selectedIds.length-1]);schedule();status(message,'ok');}catch(error){status(error.message,'error');schedule();}}
  async function commitMulti(operation,message,nextIds){var p=project();if(!p)return;try{var next=await api('/api/studio/multi-object',{method:'POST',body:JSON.stringify({designId:p.document.id,operation:operation})});window.__creativeStudioLoadProject(next);selectedIds=(nextIds||[]).filter(function(id){return next.document.layers.some(function(layer){return layer.id===id;});});if(selectedIds.length)selectPrimary(selectedIds[selectedIds.length-1]);else{var first=primary();if(first)selectedIds=[first.id];}schedule();status(message,'ok');}catch(error){status(error.message,'error');schedule();}}
  function modifier(event){return event.shiftKey||event.metaKey||event.ctrlKey;}
  function layerHit(event){var target=event.target;if(!target||!target.closest)return null;if(target.closest('#studioTransformControls')||target.closest('#studioMultiSelectionControls')||target.closest('#studioSmartGuides'))return null;if(target.closest('[data-eye]')||target.closest('[data-lock]'))return null;var row=target.closest('[data-select-layer]');if(row)return {id:row.dataset.selectLayer,canvas:false};var visual=target.closest('[data-layer-id]');if(visual&&visual.id!=='studioTransformControls')return {id:visual.getAttribute('data-layer-id'),canvas:Boolean(target.closest('#artboard'))};return null;}
  function beginDrag(event){var layers=chosen(),d=doc();if(!d||!arrangeable(layers))return false;var point=svgPoint(event),frame=bounds(layers);drag={start:point,frame:frame,dx:0,dy:0,guides:[]};document.body.style.cursor='move';event.preventDefault();event.stopPropagation();schedule();return true;}
  function pointerDown(event){var hit=layerHit(event);if(!hit||!hit.id)return;if(modifier(event)){event.preventDefault();if(selectedIds.includes(hit.id)){if(selectedIds.length>1)selectedIds=selectedIds.filter(function(id){return id!==hit.id;});}else{selectedIds.push(hit.id);}setTimeout(function(){if(selectedIds.length)selectPrimary(selectedIds[selectedIds.length-1]);schedule();},0);return;}if(hit.canvas&&selectedIds.length>1&&selectedIds.includes(hit.id)){beginDrag(event);return;}selectedIds=[hit.id];setTimeout(schedule,0);}
  function pointerMove(event){if(!drag)return;var d=doc();if(!d)return;var point=svgPoint(event);var x=drag.frame.x+(point.x-drag.start.x),y=drag.frame.y+(point.y-drag.start.y);var snapped=smartSnap(drag.frame,x,y,d);drag.dx=Math.round(snapped.x-drag.frame.x);drag.dy=Math.round(snapped.y-drag.frame.y);drag.guides=snapped.guides;var controls=$('artboard').querySelector('#studioMultiSelectionControls');if(controls)controls.setAttribute('transform','translate('+drag.dx+' '+drag.dy+')');drawSmartGuides($('artboard'),d,drag.guides);}
  function pointerUp(){if(!drag)return;var current=drag;drag=null;document.body.style.cursor='';var svg=$('artboard'),guideLayer=svg&&svg.querySelector('#studioSmartGuides');if(guideLayer)guideLayer.remove();if(current.dx||current.dy){commit({type:'MOVE_LAYERS',layerIds:selectedIds.slice(),deltaX:current.dx,deltaY:current.dy},'Selected layers moved together.');}else schedule();}
  function align(value){var layers=chosen();if(!arrangeable(layers))return;commit({type:'ALIGN_LAYERS',layerIds:selectedIds.slice(),alignment:value},'Selected layers aligned '+value+'.');}
  function distribute(axis){var layers=chosen();if(!arrangeable(layers)||layers.length<3)return;commit({type:'DISTRIBUTE_LAYERS',layerIds:selectedIds.slice(),axis:axis},'Selected layers distributed '+axis+'ly.');}
  function group(){var layers=chosen();if(!groupable(layers))return;var id='group-'+Date.now();commit({type:'GROUP_LAYERS',layerIds:selectedIds.slice(),groupLayerId:id,name:'Group'},'Selected layers grouped.',[id]);}
  function ungroup(){var layers=chosen();if(layers.length!==1||layers[0].type!=='group')return;var children=layers[0].childLayerIds.slice();commit({type:'UNGROUP_LAYERS',layerId:layers[0].id},'Group removed; child layers preserved.',children);}
  function duplicateSelected(){var layers=chosen();if(!multiMutable(layers,'duplicate'))return;var stamp=Date.now(),newIds=selectedIds.map(function(id,index){return id+'-copy-'+stamp+'-'+index;});commitMulti({type:'DUPLICATE_LAYERS',layerIds:selectedIds.slice(),newLayerIds:newIds,offsetX:20,offsetY:20},'Selected layers duplicated in one revision.',newIds);}
  function deleteSelected(){var layers=chosen();if(!multiMutable(layers,'delete'))return;commitMulti({type:'DELETE_LAYERS',layerIds:selectedIds.slice()},'Selected layers deleted in one revision.',[]);}
  function editableTarget(target){return target&&(/^(INPUT|TEXTAREA|SELECT)$/.test(target.tagName)||target.isContentEditable);}
  function keyboard(event){if(editableTarget(event.target)||event.defaultPrevented)return;var layers=chosen(),d=doc();if(!d)return;if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==='g'){event.preventDefault();if(event.shiftKey)ungroup();else group();return;}if(selectedIds.length>=2&&(event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==='d'&&multiMutable(layers,'duplicate')){event.preventDefault();duplicateSelected();return;}if(selectedIds.length>=2&&(event.key==='Delete'||event.key==='Backspace')&&multiMutable(layers,'delete')){event.preventDefault();deleteSelected();return;}if(selectedIds.length<2||!arrangeable(layers))return;if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(event.key)){event.preventDefault();var amount=event.shiftKey?10:1;var frame=bounds(layers);var x=frame.x+(event.key==='ArrowLeft'?-amount:event.key==='ArrowRight'?amount:0);var y=frame.y+(event.key==='ArrowUp'?-amount:event.key==='ArrowDown'?amount:0);var snapped=smartSnap(frame,x,y,d);commit({type:'MOVE_LAYERS',layerIds:selectedIds.slice(),deltaX:Math.round(snapped.x-frame.x),deltaY:Math.round(snapped.y-frame.y)},'Selected layers nudged '+(event.shiftKey?'10':'1')+' px.');}}
  function bind(){document.addEventListener('pointerdown',pointerDown,true);document.addEventListener('pointermove',pointerMove,true);document.addEventListener('pointerup',pointerUp,true);document.addEventListener('keydown',keyboard,true);document.querySelectorAll('[data-align]').forEach(function(button){button.addEventListener('click',function(){align(button.dataset.align);});});document.querySelectorAll('[data-distribute]').forEach(function(button){button.addEventListener('click',function(){distribute(button.dataset.distribute);});});var groupButton=$('groupSelectionBtn'),ungroupButton=$('ungroupSelectionBtn'),duplicateButton=$('duplicateSelectionBtn'),deleteButton=$('deleteSelectionBtn');if(groupButton)groupButton.addEventListener('click',group);if(ungroupButton)ungroupButton.addEventListener('click',ungroup);if(duplicateButton)duplicateButton.addEventListener('click',duplicateSelected);if(deleteButton)deleteButton.addEventListener('click',deleteSelected);var meta=$('designMeta');if(meta&&window.MutationObserver)new MutationObserver(function(){syncSingle();schedule();}).observe(meta,{childList:true,subtree:true});syncSingle();schedule();}
  bind();
})();
</script>`;
  html = injectBefore(html, "</body>", script);
  return html;
}
